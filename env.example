# Port (Heroku sets this automatically)
PORT=3000

# Google Drive Service Account Credentials (JSON format)
# For local development: use credentials.json file in project root
# For Heroku: set this environment variable with the entire JSON content
# Example: GOOGLE_CREDENTIALS='{"type":"service_account","project_id":"your-project",...}'
GOOGLE_CREDENTIALS=

